#!/usr/bin/env bash

session=$(tmux list-sessions -F '#S' 2>/dev/null | sk --prompt='tmux session> ')

if [ -n "$session" ]; then
    if [ -n "$TMUX" ]; then
        tmux switch-client -t "$session"
    else
        tmux attach-session -t "$session"
    fi
fi
